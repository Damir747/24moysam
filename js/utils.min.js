const findElement = (container, selector) => { if (container === null) { return null } return container.querySelector(selector) }; const findElementAll = (container, selector) => { if (container === null) { return [] } return container.querySelectorAll(selector) }; (function burgerMenu() { const iconMenu = findElement(document, '.menu__icon'); const menuBody = findElement(document, '.menu__body'); if (iconMenu && menuBody) { iconMenu.addEventListener("click", function (evt) { document.body.classList.toggle('_lock'); iconMenu.classList.toggle('_active'); menuBody.classList.toggle('_active') }) } function showBoundingClientRect() { } document.addEventListener('scroll', showBoundingClientRect); const menuLinks = findElementAll(document, '.menu__link[data-goto]'); if (menuLinks.length > 0) { menuLinks.forEach(menuLink => { menuLink.addEventListener('click', onMenuLinkClick) }); function onMenuLinkClick(evt) { const menuLink = evt.target; const gotoBlock = findElement(document, menuLink.dataset.goto); if (menuLink.dataset.goto && gotoBlock) { const gotoBlockValue = gotoBlock.getBoundingClientRect().top + pageYOffset - findElement(document, '.header_main').offsetHeight; if (iconMenu.classList.contains('_active')) { document.body.classList.toggle('_lock'); if (iconMenu && menuBody) { iconMenu.classList.toggle('_active'); menuBody.classList.toggle('_active') } } window.scrollTo({ top: gotoBlockValue, behavior: "smooth" }); evt.preventDefault() } } } })(); const photoGallerySwiper = new Swiper('.photo-gallery-swiper', { loop: true, spaceBetween: 10, navigation: { nextEl: '.photo-gallery-next', prevEl: '.photo-gallery-prev', }, pagination: { el: '.photo-gallery-pagination', clickable: true, }, autoplay: { delay: 3000, disableOnInteraction: false, }, breakpoints: { 480: { slidesPerView: 1, }, 640: { slidesPerView: 2, }, 920: { slidesPerView: 3, } } });